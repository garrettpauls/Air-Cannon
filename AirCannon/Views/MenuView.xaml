<UserControl x:Class="AirCannon.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Menu x:Name="mMenu">
        <MenuItem Header="_File">
            <MenuItem Header="_New" />
            <MenuItem Header="_Save" />
            <MenuItem Header="Save _As..." />
            <MenuItem Header="Open..." />
            <Separator />
            <MenuItem Header="E_xit" />
        </MenuItem>
        <MenuItem Header="_Maintenance">
            <MenuItem Header="Add Launcher" />
            <MenuItem Header="Add Launch Group" />
            <Separator />
            <MenuItem Header="Remove Current Launcher" />
            <MenuItem Header="Remove Current Launch Group" />
        </MenuItem>
    </Menu>
</UserControl>
